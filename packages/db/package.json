{
  "name": "@tote/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema, migrations, and the Postgres-backed LedgerStore.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@tote/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "fast-check": "^3.23.1",
    "prisma": "^5.22.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
