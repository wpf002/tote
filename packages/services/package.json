{
  "name": "@tote/services",
  "version": "0.0.0",
  "private": true,
  "description": "Application services: billing loop, payments, purse, import — the Phase 1/2 flows over the ledger.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@tote/core": "workspace:*",
    "@tote/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "fast-check": "^3.23.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
